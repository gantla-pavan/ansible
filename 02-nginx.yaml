- name: install and start nginx
  hosts: web
  tasks:
  - name: install nginx
    ansible.builtin.packages:
      name: nginx
      state: present

  - name: start nginx
    ansible.builtin.service:
      name: nginx
      state: started
      enabled: yes


      